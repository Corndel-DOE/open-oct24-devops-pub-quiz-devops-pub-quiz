### Run the below command for coloured outputs - better experience:

```pip3 install -r requirements.txt```

<br>
<br>

### There is a shebang line so you should be able to run the script with:

```./pub-quiz.py```
